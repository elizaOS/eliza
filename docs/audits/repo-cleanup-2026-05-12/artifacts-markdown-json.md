# Repo Cleanup Audit: Markdown, JSON, Generated Data, and Ignore Candidates

Date: 2026-05-12

Scope: repository scan from `/Users/shawwalters/eliza-workspace/milady/eliza`, excluding `node_modules`, `.git`, `.turbo`, `dist`, `build`, `.next`, and `.claude/worktrees` directories. This report is intentionally conservative: uncertain items are marked review-needed, not delete.

## Summary

- `packages/training` is the largest artifact area found, about 130G. Most of that is already covered by `packages/training/.gitignore` via `data/`, `.venv/`, `local-corpora/`, `checkpoints/`, and `wandb/`.
- `packages/benchmarks` is about 6.1G. The clearest local/generated candidates are nested virtualenvs, `node_modules`, Rust `target`, local DB state, benchmark result outputs, and cloned SWE workspaces.
- `reports` is about 473M, almost all under ignored `reports/ai-qa/` and `reports/apps-manual-qa/`. Only root release/audit summaries appear tracked.
- Root and package ignore rules already cover many high-volume local artifacts: `logs`, `*.log`, archive extensions, `reports/ai-qa/`, `reports/apps-manual-qa/`, `reports/porting/`, `packages/training/data/`, and several package-local build caches.
- There are existing dirty changes in `packages/benchmarks`, including `packages/benchmarks/benchmark_results/latest/index.json`; do not overwrite or clean these without coordinating with the current owner.

## Candidates

| Path | Category | Reason | Recommendation | Risk | Validation needed |
|---|---|---|---|---|---|
| `reports/ai-qa/` | generated report artifacts | Timestamped AI QA captures; about 468M; already ignored by `.gitignore`. | Keep ignored. Delete locally only after preserving any needed evidence. | Low | Confirm no active QA thread depends on screenshots/manifests in this directory. |
| `reports/apps-manual-qa/` | generated report artifacts | Manual QA screenshots/contact sheets and JSON reports; about 3.9M; already ignored. | Keep ignored. Delete locally after QA signoff. | Low | Confirm final visual QA evidence has been copied to the intended audit/report location. |
| `reports/porting/` | generated/temporary report artifacts | Porting run outputs are ignored; small but timestamped and run-specific. | Keep ignored; review before deletion because some files are human-readable notes. | Medium | Check whether any porting summary should move into `docs/porting/` before cleanup. |
| `reports/eliza1-release-gates/*.json` | release gate JSON reports | Timestamped gate outputs are tracked root reports, likely generated from release checks. | Review-needed. Consider moving future gate outputs under ignored `reports/eliza1-release-gates/` with only curated summaries tracked. | Medium | Ask release owner whether these JSONs are canonical evidence for 2026-05-12 readiness. |
| `reports/barrel-audit.{md,json}` | audit report artifacts | Root audit outputs are tracked and generated-looking, but may be current cleanup evidence. | Review-needed; keep unless superseded by `docs/audits` entry. | Medium | Compare against current barrel/path-alias cleanup tasks and references. |
| `reports/path-alias-audit.{md,json}` | audit report artifacts | Same pattern as barrel audit; small tracked generated-looking files. | Review-needed; keep unless migrated to `docs/audits`. | Medium | Confirm whether tooling reads the JSON or humans only read the Markdown. |
| `reports/eliza1-release-readiness-2026-05-12.md` | release report | Human-readable readiness report in root `reports`; likely intentional evidence. | Review-needed; keep or move to `docs/audits`/release docs by owner decision. | Medium | Confirm release documentation location convention. |
| `docs/audits/lifeops-2026-05-09/` | markdown audit corpus | Large set of historical audit notes. They are docs, not build output, but may be superseded. | Keep; review only for archival consolidation. | Medium | Check links from `AGENTS.md`, LifeOps README, and cleanup docs before moving/removing. |
| `docs/audits/lifeops-2026-05-11/` | markdown/json audit corpus | Dense historical LifeOps benchmark/audit notes plus JSON artifacts such as `action-collisions.json`, `retrieval-funnel.json`, and `prompts-manifest.json`. | Keep; review JSON files as generated evidence, not deletion candidates by default. | Medium | Verify whether any scripts consume the JSON files; otherwise mark archival. |
| `docs/audits/*.html` | generated/rendered report artifacts | Standalone rendered audit reports: `response-handler-and-evaluator-systems-2026-05-11.html`, `lifeops-trajectories-eliza-hermes-openclaw-2026-05-12.html`. | Review-needed. Prefer source Markdown plus ignored rendered HTML if reproducible. | Medium | Confirm whether HTML is the only copy or required for visual review. |
| `docs/audits/repo-cleanup-2026-05-11/` | cleanup audit docs | Prior cleanup wave reports and validation notes. | Keep as historical context for current cleanup. | Low | None unless archiving old cleanup waves. |
| `docs/benchmarks/*.md` | benchmark docs | Small, human-authored docs. | Keep. | Low | None. |
| `docs/training/*.md` | training docs | Small, human-authored training architecture/operations docs. | Keep. | Low | None. |
| `packages/training/data/` | generated/training data | About 127G under `data/raw`, `data/normalized`, `data/final`, and `data/native`; ignored by `packages/training/.gitignore`. | Keep ignored. Do not delete without explicit training owner approval. | High | Confirm whether current training jobs, manifests, or release reports depend on local data snapshots. |
| `packages/training/data/raw/` | downloaded raw training corpora | Includes very large downloaded corpora such as `glm-51-reasoning-1m`, `kimi-k25-reasoning-1m`, `agent-trove`, and others. | Keep ignored; candidate for external artifact storage, not repo tracking. | High | Validate dataset source URLs/checksums and reproducible download script before deletion. |
| `packages/training/data/normalized/` | generated training data | Normalized JSONL/errors outputs. | Keep ignored; delete only if normalization is reproducible. | High | Run/verify normalization pipeline and manifests. |
| `packages/training/data/final/` | generated training data | Final train/test/val JSONL and manifest. | Keep ignored; treat as generated release artifact. | High | Confirm whether these exact splits are needed for reproducibility before removal. |
| `packages/training/local-corpora/` | local corpora/workspaces | About 2.0G; ignored by `packages/training/.gitignore`; contains cloned/source corpora. | Keep ignored. Delete locally only after confirming no active training job uses it. | High | Confirm source provenance and regeneration path. |
| `packages/training/.venv/` | local environment | About 1.0G; ignored by package `.gitignore`. | Keep ignored; safe local cleanup when no shell uses it. | Low | Confirm no active process uses the venv. |
| `packages/training/reports/*.json` and `*.md` | training benchmark/release reports | Tracked reports and manifests for Eliza-1 training. Some JSON is generated evidence. | Review-needed; keep current release evidence. Consider policy: track curated Markdown, ignore bulky raw eval blobs. | Medium | Ask training/release owner which reports are canonical. |
| `packages/training/benchmarks/*.md` | benchmark planning/status docs | Human-readable optimization and gate docs. | Keep. | Low | None. |
| `plugins/app-training/datasets/*.jsonl` and `*.meta.json` | training datasets | Small checked-in action planner datasets and metadata, about 7.1M total. | Keep unless superseded; review for dataset lineage and duplication. | Medium | Confirm tests/examples depend on these exact files. |
| `packages/inference/verify/fixtures/*.json` | verification fixtures | Small deterministic fixture set, about 1.7M total. | Keep. | Low | Confirm fixture generator, if any, before replacing. |
| `packages/inference/reports/`, `packages/inference/verify/bench_results/`, `packages/inference/verify/hardware-results/`, `packages/inference/verify/android-vulkan-smoke/` | generated inference outputs | Already listed in root `.gitignore` as generated inference verification output/hardware evidence. | Keep ignored; delete locally after evidence is summarized. | Low | Confirm no pending inference PR uses the local artifacts. |
| `packages/benchmarks/HyperliquidBench/target/` | build output | Rust target directory, about 3.1G; ignored by package `.gitignore`. | Keep ignored; safe local cleanup when no build is running. | Low | Run `cargo build/test` later if cleaned and needed. |
| `packages/benchmarks/loca-bench/.venv/` | local environment | About 871M. | Add/confirm ignore coverage for benchmark virtualenvs if not already package-local. Delete locally when idle. | Low | Confirm no active benchmark run uses it. |
| `packages/benchmarks/compactbench/.venv/` | local environment | About 75M. | Add/confirm ignore coverage. Delete locally when idle. | Low | Confirm no active benchmark run uses it. |
| `packages/benchmarks/context-bench/.venv/` | local environment | About 57M. | Add/confirm ignore coverage. Delete locally when idle. | Low | Confirm no active benchmark run uses it. |
| `packages/benchmarks/hermes-adapter/.venv/` | local environment | About 14M. | Add/confirm ignore coverage. Delete locally when idle. | Low | Confirm no active benchmark run uses it. |
| `packages/benchmarks/configbench/node_modules/` | dependency install | About 194M; ignored by package `.gitignore`. | Keep ignored; safe local cleanup when no benchmark is running. | Low | Reinstall dependencies if needed. |
| `packages/benchmarks/evm/skill_runner/node_modules/` | dependency install | About 99M. | Add/confirm ignore coverage if absent. Delete locally when idle. | Low | Reinstall dependencies if needed. |
| `packages/benchmarks/swe-bench-workspace/` | cloned/generated benchmark workspace | About 191M, includes nested `.git`; appears to be workspace state rather than source. | Review-needed. Prefer ignore and regenerate from benchmark scripts. | Medium | Confirm no benchmark fixture requires this exact checked-out workspace. |
| `packages/benchmarks/swe_bench/swe-bench-workspace/` | cloned/generated benchmark workspace | Duplicate-looking SWE workspace path, about 191M. | Review-needed. Prefer ignore and regenerate. | Medium | Compare with `packages/benchmarks/swe-bench-workspace/` and benchmark configs. |
| `packages/benchmarks/benchmark_results/latest/*.json` | benchmark result output | Tracked latest result files; `latest/index.json` currently has user/other edits. | Review-needed. Do not delete or overwrite. Consider tracking only blessed baselines and ignoring transient latest outputs. | High | Coordinate with current benchmark owner; inspect dirty diff before any policy change. |
| `packages/benchmarks/benchmark_results/baselines/*.json` | benchmark baseline fixtures | Baseline JSONs look intentional and reusable. | Keep. | Low | None unless benchmark registry changes. |
| `packages/benchmarks/compactbench/results-*.jsonl` | benchmark result output | Tracked result JSONL files; likely generated examples or baselines. | Review-needed. Keep unless superseded by baseline fixtures. | Medium | Confirm whether tests/readme reference these result files. |
| `packages/benchmarks/framework/results/` | benchmark result output | Package `.gitignore` ignores `results/*.json` and `results/*.md` while preserving `.gitkeep`; untracked generated result JSONs were visible. | Keep ignore policy. Delete local generated results after benchmark owner signoff. | Low | Confirm no active run uses untracked result files. |
| `packages/benchmarks/qwen-claw-bench/data/` | benchmark dataset corpus | Large intentional benchmark data with many Markdown/JSON/JSONL assets. | Keep; not a cleanup deletion candidate without dataset owner review. | High | Validate licensing/provenance and whether data should be externalized. |
| `packages/benchmarks/claw-eval/tasks/` | benchmark fixtures | Intentional task fixtures, including JSON/Markdown data. | Keep. | High | Confirm package test expectations before pruning any task assets. |
| `packages/benchmarks/skillsbench/tasks/` | benchmark fixtures | Intentional benchmark tasks, some large media/data. | Keep; review only for storage strategy. | High | Confirm benchmark coverage requirements and external artifact availability. |
| `packages/benchmarks/loca-bench/gem/envs/**` | benchmark fixtures/generated test data | Many JSON/JSONL files are ground truth or initial workspace fixtures. | Keep. | High | Confirm benchmark fixture contracts before any removal. |
| `packages/benchmarks/nl2repo/test_files/**/test_files.json` and `test_commands.json` | benchmark fixtures | Intentional generated-looking fixture manifests for many package tasks. | Keep. | High | Confirm nl2repo harness reads these files. |
| `packages/benchmarks/voicebench-quality/**/fixtures/*.jsonl` | benchmark fixtures | Evaluation fixture datasets. | Keep. | High | Confirm fixture licensing and test coverage. |
| `packages/benchmarks/voiceagentbench/fixtures/*.jsonl` | benchmark fixtures | Existing dirty state: one deleted `mock_tasks.jsonl`, one untracked `test_tasks.jsonl`. | Review-needed. Do not touch. | High | Coordinate with current editor and inspect intended rename/replacement. |
| `packages/app/ios/App/App/public/**/*.gz` | bundled mobile assets | Duplicated compressed app assets such as VRM/FBX and tarballs. They are binary-ish but likely app bundle inputs. | Review-needed; do not delete. Consider dedup/build-copy strategy. | High | Confirm Capacitor/iOS build requires checked-in copied assets. |
| `packages/app/android/app/src/main/assets/public/**/*.gz` | bundled mobile assets | Compressed Android app assets. Likely required for mobile app packaging. | Review-needed; do not delete. Consider source-of-truth/dedup policy. | High | Confirm Android build asset pipeline and whether assets are generated copies. |
| `plugins/app-companion/public/**/*.gz` | runtime media assets | VRM/FBX/GLB compressed animation/model assets. | Keep as product assets unless licensing/storage policy says otherwise. | Medium | Confirm asset provenance and whether duplicates exist in mobile package copies. |
| `packages/app/android/.gradle/` | local build cache | Gradle binary cache files surfaced by scan; should be ignored by package `.gitignore` via `/android/`. | Keep ignored; safe local cleanup when no Gradle build is running. | Low | Confirm no local build process is active. |
| `cloud/apps/api/.wrangler/` | local/generated Cloudflare state | SQLite state and dry-run/tmp bundles; ignored by `cloud/.gitignore`. | Keep ignored; safe local cleanup when no Wrangler dev/test is running. | Low | Confirm no active Wrangler process depends on state. |
| `.eliza/.pgdata/` and `cloud/.eliza/.pgdata*/` | local database state | PostgreSQL WAL/state files. | Review ignore coverage; local cleanup only after stopping services and backing up needed state. | Medium | Confirm services are stopped and state is disposable. |
| `.benchmark-logs/`, `.test-logs/`, `.codex-test-logs/`, `logs/` | logs | Log outputs are ignored by `*.log` or `logs`. | Keep ignored; safe local cleanup after triage. | Low | Preserve failing-run logs referenced by open work. |
| `.tmp/` | local temporary workspace | Contains large local prototype environment artifacts from scan. | Add/confirm ignore coverage for `.tmp/`; delete locally when idle. | Low | Confirm no active prototype process uses it. |
| `test/mocks/mockoon/.mockoon-logs/` | local mock server logs | Log output under test mocks. | Already covered by `*.log`; keep ignored. | Low | None beyond preserving needed debug logs. |
| `*.zip`, `*.tar`, `*.tar.gz`, `*.tgz`, `*.7z` | archive candidates | Root `.gitignore` already ignores common archive formats. | Keep ignore rules. Review any tracked archive individually if discovered later. | Medium | Use `git ls-files` for tracked archives before making deletion proposals. |
| `.DS_Store` under data directories | OS metadata | Seen under `packages/training/data/`, which is ignored; possible pollution if present elsewhere. | Ensure `.DS_Store` is globally ignored if not already. Delete local copies. | Low | Run a focused tracked-file check for `.DS_Store`. |

## Ignore-List Candidates

These are candidates for future ignore-list tightening. No ignore files were edited for this report.

| Pattern | Category | Reason | Recommendation | Risk | Validation needed |
|---|---|---|---|---|---|
| `.tmp/` | local temporary workspace | Large temporary prototype artifacts appeared under `.tmp/`. | Add to root ignore if not intentionally tracked. | Low | Check `git ls-files .tmp` is empty. |
| `.benchmark-logs/`, `.test-logs/`, `.codex-test-logs/` | logs | Currently covered by `*.log`, but directory ignores would make intent explicit. | Add explicit directory ignores. | Low | Check no non-log files in those dirs should be tracked. |
| `.eliza/.pgdata/`, `cloud/.eliza/.pgdata*/` | local database state | Local DB/WAL state is not source. | Add explicit ignore patterns if absent. | Medium | Confirm no test fixture relies on checked-in DB state. |
| `**/.venv/` | local Python envs | Multiple package-local virtualenvs under benchmarks/training. | Confirm root/package ignores cover nested venvs. | Low | `git ls-files '**/.venv/**'` should remain empty. |
| `packages/benchmarks/**/node_modules/` | dependency installs | Nested benchmark dependency installs surfaced outside root-level `node_modules`. | Confirm root `node_modules/` semantics or add package-level rules where needed. | Low | `git ls-files 'packages/benchmarks/**/node_modules/**'` should remain empty. |
| `packages/benchmarks/**/target/` | Rust build outputs | HyperliquidBench has a large target dir. | Keep package ignore; consider broader benchmark target ignore. | Low | Ensure no Rust fixture directory named `target` is source data. |
| `packages/benchmarks/**/swe-bench-workspace/` | generated benchmark workspaces | Cloned benchmark workspaces are large and reproducible-looking. | Review and ignore if generated by harness. | Medium | Confirm regeneration command and no unique local patches. |
| `packages/benchmarks/**/results/*.json`, `packages/benchmarks/**/results/*.md` | benchmark outputs | Some packages already ignore result outputs. | Standardize per benchmark package. | Medium | Distinguish generated outputs from blessed baselines. |
| `reports/eliza1-release-gates/*.json` | generated release gate JSON | Current timestamped root JSON outputs look generated. | Consider ignoring future timestamped gate blobs while tracking curated release summaries. | Medium | Confirm release evidence policy. |

## Prioritized TODO

1. Confirm ownership of dirty benchmark files before any cleanup, especially `packages/benchmarks/benchmark_results/latest/index.json` and the `voiceagentbench/fixtures` JSONL change.
2. Decide artifact retention policy for `reports/`: keep curated root summaries, keep `reports/ai-qa/` and `reports/apps-manual-qa/` ignored, and decide whether future `reports/eliza1-release-gates/*.json` should be ignored or archived.
3. Ask training owners to document regeneration/source-of-truth for `packages/training/data/` and `packages/training/local-corpora/` before deleting any local 130G training data.
4. Standardize benchmark ignores for virtualenvs, nested dependency installs, Rust `target`, generated `results`, and SWE workspaces.
5. Review mobile/public compressed asset duplication across `plugins/app-companion/public`, `packages/app/ios/App/App/public`, and `packages/app/android/app/src/main/assets/public`; do not delete until the mobile build asset pipeline is understood.
6. Add explicit ignore rules for `.tmp/`, local log directories, and local DB state if tracked-file checks confirm they are purely local artifacts.
7. For generated HTML/JSON audit files under `docs/audits`, decide whether Markdown source is canonical and rendered artifacts should become ignored outputs.
