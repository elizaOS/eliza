# Phase 2 Validation - Tests

Date: 2026-05-11
Worker: Phase 2 validation worker C
Workspace: `/Users/shawwalters/eliza-workspace/eliza/eliza`
Bun: `/Users/shawwalters/.bun/bin/bun` version `1.3.13`

## Summary

Overall result: FAIL.

Root `test:ci` failed in `@elizaos/app (packages/app)#test:e2e`.
Standalone root `test:e2e` was feasible enough to run, but it also failed in
`@elizaos/app (packages/app)#test:e2e`.

Primary reproducible blocker: `packages/app/test/ui-smoke/game-apps.spec.ts`
cannot find visible launch buttons for `Defense of the Agents` and
`ClawVille`.

Secondary non-reproduced blocker from `test:ci`: `app-vincent @ desktop dark`
failed in `packages/app/test/ui-smoke/ai-qa-capture.spec.ts` with Playwright
artifact write errors plus a 180s timeout. The same Vincent capture passed in
the standalone `test:e2e` rerun.

## Commands

| Command | Exit | Result | Notes |
| --- | ---: | --- | --- |
| `/Users/shawwalters/.bun/bin/bun run test:ci` | 1 | FAIL | Failed during `@elizaos/app (packages/app)#test:e2e`. Earlier package tests passed or skipped as expected. |
| `/Users/shawwalters/.bun/bin/bun run test:e2e` | 1 | FAIL | Feasible to start and complete; failed in the same app e2e package on the two game-app smoke tests. |

Supporting read-only diagnostics:

| Command | Exit | Notes |
| --- | ---: | --- |
| `/Users/shawwalters/.bun/bin/bun --version` | 0 | `1.3.13`. |
| `git status --short` | 0 | Used before and after to identify that the shared worktree was already dirty and changed during validation. |
| `df -h .` | 0 | After `test:ci`: volume reported 16 GiB available but 100% capacity; after standalone `test:e2e`: 92 GiB available, 98% capacity. |
| `du -sh packages/app/test-results` | 0 | Playwright artifacts totaled 102 MiB after both runs. |
| `git status --short --ignored packages/app/test-results packages/app/playwright-report test-results` | 0 | `packages/app/test-results/` is ignored and was generated by Playwright. |

## `test:ci` Details

Command:

```sh
/Users/shawwalters/.bun/bin/bun run test:ci
```

Root script:

```text
TEST_LANE=pr node scripts/run-all-tests.mjs --no-cloud
```

Result: exit 1.

Progress before failure:

- Local PostgreSQL was unavailable, so plugin-sql Postgres-only suites were skipped.
- `@elizaos/cloud-sdk (cloud/packages/sdk)#test`: pass, 1 test file passed and 1 skipped, 4 tests passed and 18 skipped.
- `@elizaos/cloud-sdk (cloud/packages/sdk)#test:e2e`: pass, 1 test file skipped, 18 tests skipped.
- `@elizaos/agent (packages/agent)#test`: pass, 38 files passed, 300 tests passed, about 68s.
- `@elizaos/app-core (packages/app-core)#test`: pass, 85 files passed and 2 skipped, 819 tests passed and 11 skipped, about 168s.
- `@elizaos/electrobun (packages/app-core/platforms/electrobun)#test`: pass, 3 files passed, 7 tests passed, about 19s.
- `@elizaos/app (packages/app)#test`: pass, 1 file passed, 2 tests passed, about 19s.
- `@elizaos/app (packages/app)#test:e2e`: fail.

First failing suite:

```text
@elizaos/app (packages/app)#test:e2e
```

First failing test:

```text
[chromium] test/ui-smoke/ai-qa-capture.spec.ts:303:9
ai-qa capture > app-vincent @ desktop dark
```

Failure details:

- Playwright reported `apiRequestContext._wrapApiCall: ENOENT` opening
  `packages/app/test-results/.playwright-artifacts-0/traces/...recording89.trace`.
- Playwright also reported `ENOENT` opening
  `packages/app/test-results/.playwright-artifacts-0/fb7411ec9268dc7fa086f75632d52d25.zip`.
- The test then hit `Test timeout of 180000ms exceeded`.
- Artifacts referenced by output:
  `test-results/ai-qa-capture-ai-qa-capture-app-vincent-desktop-dark-chromium/test-failed-1.png`
  and
  `test-results/ai-qa-capture-ai-qa-capture-app-vincent-desktop-dark-chromium/error-context.md`.

Additional `test:ci` failures:

```text
[chromium] test/ui-smoke/game-apps.spec.ts:327:3
Defense of the Agents route exposes playable controls and chat
```

- Failed waiting 60s for
  `getByRole('button', { name: 'Launch Defense of the Agents' })`.
- Playwright additionally reported `ENOSPC: no space left on device` while
  writing
  `packages/app/test-results/game-apps-Defense-of-the-A-2e237--playable-controls-and-chat-chromium/trace.zip`.

```text
[chromium] test/ui-smoke/game-apps.spec.ts:327:3
ClawVille route exposes playable controls and chat
```

- Failed waiting 60s for
  `getByRole('button', { name: 'Launch ClawVille' })`.

Final `test:ci` app e2e counts:

- 3 failed.
- 1 skipped.
- 177 passed.
- Playwright duration: 14.0m for the app e2e package.
- Root runner stopped with
  `Error: @elizaos/app (packages/app)#test:e2e failed with exit code 1`.

## `test:e2e` Details

Command:

```sh
/Users/shawwalters/.bun/bin/bun run test:e2e
```

Root script:

```text
TEST_LANE=pr node scripts/run-all-tests.mjs --only=e2e
```

Result: exit 1.

Feasibility:

- Feasible to start and complete in this environment.
- Local PostgreSQL was unavailable, so plugin-sql Postgres-only suites remained skipped.
- `@elizaos/cloud-sdk (cloud/packages/sdk)#test:e2e` skipped cleanly.
- `@elizaos/app (packages/app)#test:e2e` launched Playwright and completed 189 tests before exiting 1.
- The earlier `app-vincent @ desktop dark` artifact failure did not reproduce; that test passed in about 598ms in the standalone e2e run.

First failing suite:

```text
@elizaos/app (packages/app)#test:e2e
```

First failing test:

```text
[chromium] test/ui-smoke/game-apps.spec.ts:327:3
Defense of the Agents route exposes playable controls and chat
```

Failure details:

- Failed waiting 60s for
  `getByRole('button', { name: 'Launch Defense of the Agents' })`.

Second failing test:

```text
[chromium] test/ui-smoke/game-apps.spec.ts:327:3
ClawVille route exposes playable controls and chat
```

Failure details:

- Failed waiting 60s for
  `getByRole('button', { name: 'Launch ClawVille' })`.

Final standalone `test:e2e` app e2e counts:

- 2 failed.
- 1 skipped.
- 186 passed.
- Playwright duration: 12.2m for the app e2e package.
- Root runner stopped with
  `Error: @elizaos/app (packages/app)#test:e2e failed with exit code 1`.

## Slow Or Hung Areas

No command required manual interruption.

Slow areas observed:

- `packages/app` Playwright UI smoke is the dominant runtime cost.
- `test:ci` app e2e ran 181 tests using 1 worker and took 14.0m.
- Standalone `test:e2e` app e2e ran 189 tests using 1 worker and took 12.2m.
- `packages/app/test/ui-smoke/ai-qa-capture.spec.ts` was explicitly reported by Playwright as a slow file in standalone e2e: 8.6m.
- Standalone `app-elizamaker` AI QA captures each took about 1.5m across desktop light, desktop dark, mobile light, and mobile dark.
- Chat mobile AI QA captures took about 30s each in standalone e2e and about 32-33s in the `test:ci` run.
- Runtime mobile checks took about 15-20s in both runs.
- `packages/app-core` unit tests were quiet for long stretches but passed in about 168s.
- `packages/agent` unit tests were quiet during startup but passed in about 68s.

Potential environment pressure:

- During `test:ci`, Playwright reported `ENOSPC: no space left on device` while writing one game-app trace.
- Immediately after `test:ci`, `df -h .` showed 16 GiB available but 100% capacity on `/System/Volumes/Data`.
- After standalone `test:e2e`, `df -h .` showed 92 GiB available and 98% capacity.
- This suggests transient APFS/workspace disk pressure during artifact generation, but the reproducible standalone failure was the missing game launch buttons, not `ENOSPC`.

## Likely Owners

Primary:

- `packages/app` UI smoke owners for `packages/app/test/ui-smoke/game-apps.spec.ts`.
- App catalog/game route owners for the `Defense of the Agents` and `ClawVille` entries and launch-button rendering.

Secondary:

- AI QA capture/Playwright artifact owners for the non-reproduced `app-vincent @ desktop dark` artifact timeout in `packages/app/test/ui-smoke/ai-qa-capture.spec.ts`.
- Vincent app UI owners if the artifact failure is masking an actual render problem, though the standalone rerun passed the Vincent capture and direct route checks passed in `test:ci`.
- Test infrastructure owners for intermittent Playwright artifact write failures under disk pressure.

## Generated Side Effects

- No source, config, or test files were intentionally edited by this worker.
- This report was created at
  `docs/audits/repo-cleanup-2026-05-11/phase-2-validation/validation-tests.md`.
- Playwright generated ignored artifacts under `packages/app/test-results/`.
- After both runs, `packages/app/test-results/` was 102 MiB.
- Post-run artifact entries included:
  - `packages/app/test-results/.last-run.json`
  - `packages/app/test-results/game-apps-ClawVille-route-exposes-playable-controls-and-chat-chromium`
  - `packages/app/test-results/.playwright-artifacts-1`
  - `packages/app/test-results/titlebar-navigation-macOS--554a2-s-macOS-traffic-light-inset-chromium`
  - `packages/app/test-results/titlebar-navigation-macOS--afdd9-le-and-title-area-draggable-chromium`
- The `test:ci` output also referenced transient failure artifact paths for `app-vincent`, `Defense of the Agents`, and `ClawVille`.
- The worktree was already dirty before validation and changed substantially while tests were running. Because other workers were active, post-run tracked changes such as `.gitignore`, `packages/app/package.json`, `packages/app/test/ui-smoke/ai-qa-capture.spec.ts`, `packages/ui/src/components/apps/*`, and other files cannot be attributed to this validation worker.

## Next Actions

1. Fix or update the game-app smoke expectations for `Defense of the Agents`
   and `ClawVille`, specifically why the app detail route does not expose
   visible `Launch ...` buttons within 60s.
2. Re-run:
   `/Users/shawwalters/.bun/bin/bun run test:e2e`.
3. After standalone e2e is green, re-run:
   `/Users/shawwalters/.bun/bin/bun run test:ci`.
4. Investigate the `app-vincent @ desktop dark` `test:ci` artifact timeout
   only if it reproduces; current evidence points to an intermittent
   Playwright artifact/disk-pressure issue.
5. Consider reducing app e2e wall time by splitting the slow
   `ai-qa-capture.spec.ts` work or enabling safe parallelism; Playwright
   reported that file alone at 8.6m in standalone e2e.
