# `action.AWAIT_PAYMENT_CALLBACK@packages/core/src/features/payments/actions/await-payment-callback.ts.param.timeoutMs.description`

- **Kind**: action-parameter
- **Owner**: packages/core
- **File**: `packages/core/src/features/payments/actions/await-payment-callback.ts:35`
- **Token count**: 15
- **Last optimized**: never
- **Action**: AWAIT_PAYMENT_CALLBACK
- **Parameter**: timeoutMs (required: no)

## Current text
```
Wait timeout in milliseconds. Defaults to 600000 (10 min).
```

## Compressed variant
```
none
```

## Usage stats (latest trajectories)
- Invocations: 0 (this prompt was not matched in any recent trajectory)

## Sample failure transcripts
None.

## Suggested edits (heuristic)
None.

## Actions
- Accept a candidate rewrite: `bun run lifeops:prompt-accept -- --id <id> --from <candidate-file>`
- Freeze (skip future optimization): `bun run lifeops:prompt-freeze -- --id <id>`
