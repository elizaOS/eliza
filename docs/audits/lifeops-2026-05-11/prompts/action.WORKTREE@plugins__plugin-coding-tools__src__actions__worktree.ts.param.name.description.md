# `action.WORKTREE@plugins/plugin-coding-tools/src/actions/worktree.ts.param.name.description`

- **Kind**: action-parameter
- **Owner**: plugins/plugin-coding-tools
- **File**: `plugins/plugin-coding-tools/src/actions/worktree.ts:55`
- **Token count**: 18
- **Last optimized**: never
- **Action**: WORKTREE
- **Parameter**: name (required: no)

## Current text
```
For action=enter, optional worktree branch/dir name. Defaults to auto-*.
```

## Compressed variant
```
none
```

## Usage stats (latest trajectories)
- Invocations: 0 (this prompt was not matched in any recent trajectory)

## Sample failure transcripts
None.

## Suggested edits (heuristic)
None.

## Actions
- Accept a candidate rewrite: `bun run lifeops:prompt-accept -- --id <id> --from <candidate-file>`
- Freeze (skip future optimization): `bun run lifeops:prompt-freeze -- --id <id>`
