# `action.WORKTREE@plugins/plugin-coding-tools/src/actions/worktree.ts.description`

- **Kind**: action-description
- **Owner**: plugins/plugin-coding-tools
- **File**: `plugins/plugin-coding-tools/src/actions/worktree.ts:65`
- **Token count**: 45
- **Last optimized**: never
- **Action**: WORKTREE
- **Similes**: ENTER_WORKTREE, EXIT_WORKTREE, GIT_WORKTREE_ADD, GIT_WORKTREE_REMOVE

## Current text
```
Manage the current git worktree stack. Choose action=enter to create and switch into an isolated worktree, or action=exit to leave the current worktree and optionally remove it.
```

## Compressed variant
```
Git worktree umbrella: action=enter/exit.
```

## Usage stats (latest trajectories)
- Invocations: 0 (this prompt was not matched in any recent trajectory)

## Sample failure transcripts
None.

## Suggested edits (heuristic)
None.

## Actions
- Accept a candidate rewrite: `bun run lifeops:prompt-accept -- --id <id> --from <candidate-file>`
- Freeze (skip future optimization): `bun run lifeops:prompt-freeze -- --id <id>`
