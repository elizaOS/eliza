# `action.TAILSCALE@plugins/plugin-tailscale/src/actions/tailscale.ts.description`

- **Kind**: action-description
- **Owner**: plugins/plugin-tailscale
- **File**: `plugins/plugin-tailscale/src/actions/tailscale.ts:277`
- **Token count**: 39
- **Last optimized**: never
- **Action**: TAILSCALE

## Current text
```
Tailscale tunnel router. Operations: start (open tunnel for a local port), stop (close active tunnel). Status reads come from the tailscaleStatus provider.
```

## Compressed variant
```
Tailscale: start tunnel, stop tunnel.
```

## Usage stats (latest trajectories)
- Invocations: 0 (this prompt was not matched in any recent trajectory)

## Sample failure transcripts
None.

## Suggested edits (heuristic)
None.

## Actions
- Accept a candidate rewrite: `bun run lifeops:prompt-accept -- --id <id> --from <candidate-file>`
- Freeze (skip future optimization): `bun run lifeops:prompt-freeze -- --id <id>`
