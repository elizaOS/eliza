# `action.ENTITY@plugins/app-lifeops/src/actions/entity.ts.param.relationshipType.description`

- **Kind**: action-parameter
- **Owner**: plugins/app-lifeops
- **File**: `plugins/app-lifeops/src/actions/entity.ts:447`
- **Token count**: 25
- **Last optimized**: never
- **Action**: ENTITY
- **Parameter**: relationshipType (required: no)

## Current text
```
Edge type label for set_relationship (e.g. manages, colleague_of, works_at, partner_of, family_of).
```

## Compressed variant
```
set_relationship edge type label e.g. manages|colleague_of|works_at|partner_of
```

## Usage stats (latest trajectories)
- Invocations: 0 (this prompt was not matched in any recent trajectory)

## Sample failure transcripts
None.

## Suggested edits (heuristic)
None.

## Actions
- Accept a candidate rewrite: `bun run lifeops:prompt-accept -- --id <id> --from <candidate-file>`
- Freeze (skip future optimization): `bun run lifeops:prompt-freeze -- --id <id>`
