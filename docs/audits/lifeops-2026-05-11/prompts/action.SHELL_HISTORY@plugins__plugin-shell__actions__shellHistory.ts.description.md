# `action.SHELL_HISTORY@plugins/plugin-shell/actions/shellHistory.ts.description`

- **Kind**: action-description
- **Owner**: plugins/plugin-shell
- **File**: `plugins/plugin-shell/actions/shellHistory.ts:68`
- **Token count**: 38
- **Last optimized**: never
- **Action**: SHELL_HISTORY
- **Similes**: CLEAR_HISTORY, CLEAR_SHELL_HISTORY, RESET_SHELL, CLEAR_TERMINAL, RESET_HISTORY, VIEW_SHELL_HISTORY, SHOW_SHELL_HISTORY, LIST_SHELL_HISTORY

## Current text
```
Shell command-history router. action=clear wipes the recorded history; action=view returns recent commands; action=disable is reserved for future use.
```

## Compressed variant
```
Shell history: clear | view | disable.
```

## Usage stats (latest trajectories)
- Invocations: 0 (this prompt was not matched in any recent trajectory)

## Sample failure transcripts
None.

## Suggested edits (heuristic)
None.

## Actions
- Accept a candidate rewrite: `bun run lifeops:prompt-accept -- --id <id> --from <candidate-file>`
- Freeze (skip future optimization): `bun run lifeops:prompt-freeze -- --id <id>`
