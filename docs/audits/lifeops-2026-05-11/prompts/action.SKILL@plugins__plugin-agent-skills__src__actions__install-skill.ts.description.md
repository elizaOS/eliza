# `action.SKILL@plugins/plugin-agent-skills/src/actions/install-skill.ts.description`

- **Kind**: action-description
- **Owner**: plugins/plugin-agent-skills
- **File**: `plugins/plugin-agent-skills/src/actions/install-skill.ts:41`
- **Token count**: 44
- **Last optimized**: never
- **Action**: SKILL
- **Similes**: INSTALL_SKILL, DOWNLOAD_SKILL, ADD_SKILL, GET_SKILL, FETCH_SKILL

## Current text
```
Install a skill from the ClawHub registry. The skill will be security-scanned before activation. Provide a skill slug or search term, e.g. "install weather" or "add github".
```

## Compressed variant
```
Install skill from ClawHub registry. Security-scanned before activation.
```

## Usage stats (latest trajectories)
- Invocations: 0 (this prompt was not matched in any recent trajectory)

## Sample failure transcripts
None.

## Suggested edits (heuristic)
None.

## Actions
- Accept a candidate rewrite: `bun run lifeops:prompt-accept -- --id <id> --from <candidate-file>`
- Freeze (skip future optimization): `bun run lifeops:prompt-freeze -- --id <id>`
