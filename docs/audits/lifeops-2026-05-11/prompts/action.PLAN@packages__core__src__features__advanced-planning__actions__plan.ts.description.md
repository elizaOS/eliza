# `action.PLAN@packages/core/src/features/advanced-planning/actions/plan.ts.description`

- **Kind**: action-description
- **Owner**: packages/core
- **File**: `packages/core/src/features/advanced-planning/actions/plan.ts:136`
- **Token count**: 30
- **Last optimized**: never
- **Action**: PLAN
- **Similes**: CREATE_PLAN, PLAN_PROJECT, GENERATE_PLAN, MAKE_PLAN, PROJECT_PLAN

## Current text
```
Project planning router. action=create generates a multi-phase plan; update/finalize/review are reserved for future use.
```

## Compressed variant
```
none
```

## Usage stats (latest trajectories)
- Invocations: 0 (this prompt was not matched in any recent trajectory)

## Sample failure transcripts
None.

## Suggested edits (heuristic)
- No compressed variant. Authors should add `descriptionCompressed` — the planner caches both shapes and falls back to the long form when the compressed one is absent.

## Actions
- Accept a candidate rewrite: `bun run lifeops:prompt-accept -- --id <id> --from <candidate-file>`
- Freeze (skip future optimization): `bun run lifeops:prompt-freeze -- --id <id>`
