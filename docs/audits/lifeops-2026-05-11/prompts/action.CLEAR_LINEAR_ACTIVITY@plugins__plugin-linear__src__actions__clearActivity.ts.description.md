# `action.CLEAR_LINEAR_ACTIVITY@plugins/plugin-linear/src/actions/clearActivity.ts.description`

- **Kind**: action-description
- **Owner**: plugins/plugin-linear
- **File**: `plugins/plugin-linear/src/actions/clearActivity.ts:22`
- **Token count**: 54
- **Last optimized**: never
- **Action**: CLEAR_LINEAR_ACTIVITY

## Current text
```
Clear the cached Linear activity log for the connected Linear account. Use when the user asks to reset, wipe, or refresh their Linear activity history before pulling a fresh view of recent issue and comment events.
```

## Compressed variant
```
clear Linear activity log
```

## Usage stats (latest trajectories)
- Invocations: 0 (this prompt was not matched in any recent trajectory)

## Sample failure transcripts
None.

## Suggested edits (heuristic)
- Compressed variant exists (25 chars vs 214 chars — 88% shorter). Consider promoting it when planner cache pressure is high.

## Actions
- Accept a candidate rewrite: `bun run lifeops:prompt-accept -- --id <id> --from <candidate-file>`
- Freeze (skip future optimization): `bun run lifeops:prompt-freeze -- --id <id>`
