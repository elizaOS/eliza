# `action.LIST_LINEAR_COMMENTS@plugins/plugin-linear/src/actions/listComments.ts.param.limit.description`

- **Kind**: action-parameter
- **Owner**: plugins/plugin-linear
- **File**: `plugins/plugin-linear/src/actions/listComments.ts:16`
- **Token count**: 15
- **Last optimized**: never
- **Action**: LIST_LINEAR_COMMENTS
- **Parameter**: limit (required: no)

## Current text
```
Maximum number of comments to return (default 25, max 100).
```

## Compressed variant
```
none
```

## Usage stats (latest trajectories)
- Invocations: 0 (this prompt was not matched in any recent trajectory)

## Sample failure transcripts
None.

## Suggested edits (heuristic)
None.

## Actions
- Accept a candidate rewrite: `bun run lifeops:prompt-accept -- --id <id> --from <candidate-file>`
- Freeze (skip future optimization): `bun run lifeops:prompt-freeze -- --id <id>`
