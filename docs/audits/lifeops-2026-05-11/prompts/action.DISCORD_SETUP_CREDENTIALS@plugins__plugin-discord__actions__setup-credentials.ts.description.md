# `action.DISCORD_SETUP_CREDENTIALS@plugins/plugin-discord/actions/setup-credentials.ts.description`

- **Kind**: action-description
- **Owner**: plugins/plugin-discord
- **File**: `plugins/plugin-discord/actions/setup-credentials.ts:513`
- **Token count**: 49
- **Last optimized**: never
- **Action**: DISCORD_SETUP_CREDENTIALS
- **Similes**: DISCORD_SETUP, DISCORD_PAIR, DISCORD_CONNECT, DISCORD_ADD_CREDENTIALS, DISCORD_CONFIGURE_SERVICE, DISCORD_CONNECT_SERVICE, DISCORD_ADD_API_KEY, DISCORD_SETUP_SERVICE

## Current text
```
Start Discord credential setup or account pairing. Guides the user through setting up API credentials for supported third-party services, validates them when possible, and stores them securely.
```

## Compressed variant
```
Set up Discord credentials.
```

## Usage stats (latest trajectories)
- Invocations: 0 (this prompt was not matched in any recent trajectory)

## Sample failure transcripts
None.

## Suggested edits (heuristic)
None.

## Actions
- Accept a candidate rewrite: `bun run lifeops:prompt-accept -- --id <id> --from <candidate-file>`
- Freeze (skip future optimization): `bun run lifeops:prompt-freeze -- --id <id>`
