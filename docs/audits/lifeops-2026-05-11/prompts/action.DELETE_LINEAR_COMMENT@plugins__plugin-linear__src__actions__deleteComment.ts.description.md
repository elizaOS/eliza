# `action.DELETE_LINEAR_COMMENT@plugins/plugin-linear/src/actions/deleteComment.ts.description`

- **Kind**: action-description
- **Owner**: plugins/plugin-linear
- **File**: `plugins/plugin-linear/src/actions/deleteComment.ts:20`
- **Token count**: 39
- **Last optimized**: never
- **Action**: DELETE_LINEAR_COMMENT

## Current text
```
Delete a specific Linear comment by its comment id. Use when the user asks to remove, retract, or erase a comment they previously left on a Linear issue.
```

## Compressed variant
```
delete Linear comment id
```

## Usage stats (latest trajectories)
- Invocations: 0 (this prompt was not matched in any recent trajectory)

## Sample failure transcripts
None.

## Suggested edits (heuristic)
None.

## Actions
- Accept a candidate rewrite: `bun run lifeops:prompt-accept -- --id <id> --from <candidate-file>`
- Freeze (skip future optimization): `bun run lifeops:prompt-freeze -- --id <id>`
