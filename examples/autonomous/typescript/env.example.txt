# ============================================================================
# Autonomous Agent Configuration - TypeScript
# Copy this file to .env and customize values
# ============================================================================

# -----------------------------------------------------------------------------
# LOCAL AI CONFIGURATION
# -----------------------------------------------------------------------------
# Directory where GGUF models are stored
# Default: ~/.eliza/models
MODELS_DIR=~/.eliza/models

# Small model for quick inference (used for autonomous loop)
# Download from: https://huggingface.co/Qwen/Qwen3-4B-GGUF
# Example: Qwen3-4B-Q4_K_M.gguf
LOCAL_SMALL_MODEL=Qwen3-4B-Q4_K_M.gguf

# Large model (optional, not used in autonomous loop by default)
LOCAL_LARGE_MODEL=Qwen3-4B-Q4_K_M.gguf

# Embedding model for vector search
LOCAL_EMBEDDING_MODEL=bge-small-en-v1.5.Q4_K_M.gguf

# Cache directory for model artifacts
CACHE_DIR=~/.eliza/cache

# -----------------------------------------------------------------------------
# SHELL CONFIGURATION
# -----------------------------------------------------------------------------
# Enable shell command execution (REQUIRED for autonomous agent)
SHELL_ENABLED=true

# Sandbox directory for shell operations (will be auto-set to examples/autonomous/sandbox)
# SHELL_ALLOWED_DIRECTORY is set automatically by the agent

# Command timeout in milliseconds
SHELL_TIMEOUT=30000

# Additional forbidden commands (comma-separated)
# The agent already blocks networking and interpreter commands
SHELL_FORBIDDEN_COMMANDS=

# -----------------------------------------------------------------------------
# AUTONOMY CONFIGURATION
# -----------------------------------------------------------------------------
# Sandbox directory where the agent operates
# Default: examples/autonomous/sandbox
SANDBOX_DIR=

# Interval between loop iterations in milliseconds
# Default: 3000 (3 seconds)
LOOP_INTERVAL_MS=3000

# Maximum number of iterations before stopping
# Default: 1000
MAX_ITERATIONS=1000

# Maximum consecutive failures before stopping
# Default: 5
MAX_CONSECUTIVE_FAILURES=5

# Number of recent memory records to include in context
# Default: 10
MEMORY_CONTEXT_SIZE=10

# Set to "false" to stop the agent
AUTONOMY_ENABLED=true

# Agent UUID (auto-generated if not set)
AGENT_ID=

# Conversation/session UUID (auto-generated if not set)
CONVERSATION_ID=

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
# Log level: trace, debug, info, warn, error, fatal
LOG_LEVEL=info
