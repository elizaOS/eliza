{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "benchmarks.schema.json",
  "title": "Benchmarks",
  "type": "object",
  "required": ["domain", "description", "goodPairs", "badPairs"],
  "additionalProperties": false,
  "properties": {
    "domain": {
      "type": "string",
      "enum": ["general", "dating", "business", "friendship"]
    },
    "description": { "type": "string" },
    "goodPairs": { "$ref": "#/$defs/pairs" },
    "badPairs": { "$ref": "#/$defs/pairs" }
  },
  "$defs": {
    "pairs": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["a", "b", "expectedScoreRange", "reason"],
        "additionalProperties": false,
        "properties": {
          "a": { "type": "integer", "minimum": 0, "maximum": 199 },
          "b": { "type": "integer", "minimum": 0, "maximum": 199 },
          "expectedScoreRange": {
            "type": "array",
            "minItems": 2,
            "maxItems": 2,
            "items": { "type": "integer", "minimum": -100, "maximum": 100 }
          },
          "reason": { "type": "string" }
        }
      }
    }
  }
}
