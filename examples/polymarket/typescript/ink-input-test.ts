/**
 * Stub for ink-based input test (optional dev flow).
 * Export so runner module loads; implementation can be added when needed.
 */
export function runInkInputTest(): void {
  // No-op when ink input test is not used
}
