#!/usr/bin/env node
/**
 * Install test dependencies for plugin-starter.
 * No-op when run in repo context; can be extended for Playwright/Cypress deps.
 * Used by test:install before running component/e2e tests.
 */
process.exit(0);

