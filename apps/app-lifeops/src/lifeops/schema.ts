/**
 * LifeOps plugin database schema.
 *
 * Stub until the relevant T9f / other-owner work lands with the real
 * drizzle schema. Intentionally empty so the Plugin `schema` field
 * resolves to an empty table set without registering any tables.
 * Replace with the real schema once that work is merged.
 */

export const lifeOpsSchema = {} as const;
