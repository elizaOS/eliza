---
name: testing
sourcePath: zk-router/skills/testing
description: Catalog entry for the requested zk-router testing skill. The requested source path is not present in the current zk-router checkout, so this placeholder tracks planned zk-router testing guidance until the real SKILL.md is added.
homepage: https://github.com/Solizardking/zk-router
tags: [zk-router, testing, smoke-tests, verification, pending-source]
---

# testing

This entry is included so the Cheshire Terminal skills hub advertises the requested zk-router `testing` skill path.

Current source status:

- Requested path: `zk-router/skills/testing`
- Local source found: no `SKILL.md` in the current zk-router checkout
- Catalog behavior: visible in `/skills` and `/api/skills` as a pending-source zk-router skill

Replace this file with the upstream `SKILL.md` when that source path becomes available.
