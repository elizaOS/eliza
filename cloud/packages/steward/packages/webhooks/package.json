{
  "name": "@stwd/webhooks",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "tsc --watch",
    "test": "bun test"
  },
  "dependencies": {
    "@stwd/shared": "workspace:*",
    "@stwd/db": "workspace:*"
  },
  "peerDependencies": {
    "drizzle-orm": "^0.45.1"
  },
  "description": "Webhook dispatch and retry queue for Steward transaction events",
  "devDependencies": {
    "@types/node": "^25.6.0"
  }
}
