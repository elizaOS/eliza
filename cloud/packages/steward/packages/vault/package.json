{
  "name": "@stwd/vault",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "tsc --watch",
    "test": "bun test"
  },
  "dependencies": {
    "@solana/web3.js": "^1",
    "@stwd/db": "workspace:*",
    "@stwd/shared": "workspace:*",
    "viem": "^2.30.0"
  },
  "peerDependencies": {
    "drizzle-orm": "^0.45.1"
  },
  "description": "AES-256-GCM encrypted keystore and transaction signing via viem",
  "devDependencies": {
    "@types/node": "^25.5.0"
  }
}
