/**
 * Re-export of the canonical mock agent server (issue #9941 — one harness). The
 * source of truth is `@elizaos/plugin-xr/simulator`; do not fork it here.
 */
export * from "../../../plugin-xr/simulator/src/mock-agent.ts";
