/**
 * Re-export of the canonical XR harness types (issue #9941 — one harness). The
 * source of truth is `@elizaos/plugin-xr/simulator`; do not redefine these here.
 */
export * from "../../../plugin-xr/simulator/src/types.ts";
