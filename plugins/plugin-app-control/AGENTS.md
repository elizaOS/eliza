# App view control

Preserve actor-bound navigation decisions, caller-scoped catalogs, cancellation and renderer delivery receipts. Recovered from the verified consolidated branch; app lifecycle/worker services remain removed. Domain records belong to their owning plugins. Build with `bun run build`; validate live navigation against the isolated preview.
