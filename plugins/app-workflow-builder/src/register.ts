/**
 * Side-effect entry point — registers the workflow builder app.
 *
 * Import this module when you want auto-registration:
 *   import "@elizaos/app-workflow-builder/register";
 */

export function registerWorkflowBuilderApp(): void {
  // Stub: workflow builder app registration
  // Full implementation pending
}

registerWorkflowBuilderApp();
