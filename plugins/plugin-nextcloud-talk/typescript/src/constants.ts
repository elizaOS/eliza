/**
 * Service name used for registration with the elizaOS runtime.
 */
export const NEXTCLOUD_TALK_SERVICE_NAME = "nextcloud-talk";

/**
 * Plugin name.
 */
export const PLUGIN_NAME = "nextcloud-talk";

/**
 * Plugin version.
 */
export const PLUGIN_VERSION = "2.0.0-alpha.1";

/**
 * Plugin description.
 */
export const PLUGIN_DESCRIPTION = "Nextcloud Talk integration for elizaOS agents";

/**
 * Maximum message length for Nextcloud Talk (approximately 32KB).
 */
export const MAX_MESSAGE_LENGTH = 32000;

/**
 * Default webhook port.
 */
export const DEFAULT_WEBHOOK_PORT = 8788;

/**
 * Default webhook path.
 */
export const DEFAULT_WEBHOOK_PATH = "/nextcloud-talk-webhook";
