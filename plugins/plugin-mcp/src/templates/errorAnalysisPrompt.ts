/** Re-export shim: the error-analysis prompt template, sourced from prompts.ts. */
import { errorAnalysisTemplate } from "@elizaos/shared/mcp/prompts";

export const errorAnalysisPrompt = errorAnalysisTemplate;
