/** Re-exports the Drizzle workflow schema tables and `workflowSchema` from ./schema. */
export {
  credentialMappings,
  embeddedCredentials,
  embeddedExecutions,
  embeddedTags,
  embeddedWorkflows,
  workflowRevisions,
  workflowSchema,
} from './schema';
