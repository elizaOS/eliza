/** Re-exports the Drizzle workflow schema tables and `workflowSchema` from ./schema. */
export {
  embeddedExecutions,
  embeddedTags,
  embeddedWorkflows,
  workflowRevisions,
  workflowSchema,
} from './schema';
