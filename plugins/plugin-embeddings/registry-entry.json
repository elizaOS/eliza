{
  "id": "embeddings",
  "name": "Embeddings",
  "description": "Canonical BAAI/bge-small-en-v1.5 TEXT_EMBEDDING provider. The endpoint must return 384-dimensional mean-pooled vectors; results are L2-normalized and mismatches fail closed.",
  "npmName": "@elizaos/plugin-embeddings",
  "version": "2.0.0-beta.0",
  "source": "bundled",
  "tags": ["ai-provider", "llm", "embedding", "embeddings"],
  "config": {
    "EMBEDDING_BASE_URL": {
      "type": "url",
      "required": false,
      "sensitive": false,
      "label": "Embedding Base Url",
      "help": "Base URL of an OpenAI-compatible /embeddings endpoint serving BAAI/bge-small-en-v1.5 with 384-dimensional mean-pooled output. Setting this (or the API key) activates the plugin.",
      "placeholder": "e.g., http://localhost:8080/v1",
      "advanced": false
    },
    "EMBEDDING_API_KEY": {
      "type": "secret",
      "required": false,
      "sensitive": true,
      "label": "Embedding Api Key",
      "help": "Bearer token for the embedding endpoint. Setting this (or the base URL) activates the plugin. Omit for local servers that need no auth.",
      "advanced": false
    },
    "EMBEDDING_MODEL": {
      "type": "string",
      "required": false,
      "sensitive": false,
      "default": "BAAI/bge-small-en-v1.5",
      "label": "Embedding Model",
      "help": "Embedding model identifier sent as the request `model` field.",
      "placeholder": "e.g., BAAI/bge-small-en-v1.5",
      "advanced": false
    },
    "EMBEDDING_POOLING": {
      "type": "string",
      "required": false,
      "sensitive": false,
      "default": "mean",
      "label": "Embedding Pooling",
      "help": "Canonical pooling mode. Values other than mean fail closed.",
      "advanced": true
    },
    "EMBEDDING_FALLBACK_BASE_URL": {
      "type": "url",
      "required": false,
      "sensitive": false,
      "label": "Embedding Fallback Base Url",
      "help": "Optional fallback OpenAI-compatible /embeddings base URL. Used once when the primary endpoint fails with a network, HTTP, or response-shape error. Does not activate the plugin by itself.",
      "placeholder": "e.g., https://api.openai.com/v1",
      "advanced": true
    },
    "EMBEDDING_FALLBACK_API_KEY": {
      "type": "secret",
      "required": false,
      "sensitive": true,
      "label": "Embedding Fallback Api Key",
      "help": "Bearer token for the fallback embedding endpoint. Omit for fallback servers that need no auth.",
      "advanced": true
    },
    "EMBEDDING_FALLBACK_MODEL": {
      "type": "string",
      "required": false,
      "sensitive": false,
      "label": "Embedding Fallback Model",
      "help": "Fallback embedding model identifier. Defaults to EMBEDDING_MODEL when unset. The fallback result must still match EMBEDDING_DIMENSIONS.",
      "placeholder": "e.g., BAAI/bge-small-en-v1.5",
      "advanced": true
    },
    "EMBEDDING_DIMENSIONS": {
      "type": "number",
      "required": false,
      "sensitive": false,
      "default": "384",
      "label": "Embedding Dimensions",
      "help": "Canonical BGE-small vector width. Values other than 384 fail closed.",
      "advanced": false
    },
    "EMBEDDING_BROWSER_URL": {
      "type": "url",
      "required": false,
      "sensitive": false,
      "label": "Embedding Browser Url",
      "help": "Browser-only server-side proxy URL for /embeddings. In a browser build the Authorization header is never sent unless this is set, so the API key stays server-side.",
      "advanced": true
    }
  },
  "render": {
    "visible": true,
    "pinTo": [],
    "style": "card",
    "icon": "Boxes",
    "group": "ai-provider",
    "groupOrder": 0,
    "actions": ["enable", "configure"]
  },
  "resources": {
    "homepage": "https://github.com/elizaOS/eliza/tree/HEAD/plugins/plugin-embeddings",
    "repository": "https://github.com/elizaOS/eliza",
    "setupGuideUrl": "https://docs.eliza.ai/plugin-setup-guide#embeddings"
  },
  "dependsOn": [],
  "kind": "plugin",
  "subtype": "ai-provider"
}
