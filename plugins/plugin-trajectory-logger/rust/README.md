# elizaOS Trajectory Logger Plugin (Rust)

Rust implementation of the elizaOS Trajectory Logger plugin.

This crate provides:

- In-memory `TrajectoryLoggerService`
- ART / GRPO formatting helpers
- Simple heuristic reward scoring

