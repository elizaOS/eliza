# @elizaos/plugin-streaming-base

Shared factory for building RTMP streaming destination plugins for elizaOS agents.

Provides `createStreamingPlugin()` and `createStreamingDestination()` factories used by platform-specific streaming plugins (Twitch, YouTube, pump.fun, X, Retake).
