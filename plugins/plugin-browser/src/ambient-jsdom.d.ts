/// <reference types="jsdom" />

// Ambient declarations bridging jsdom into our type checker. Re-establishes
// the file referenced by ../workspace/browser-workspace-types.ts after the
// upstream rename pass dropped it.
