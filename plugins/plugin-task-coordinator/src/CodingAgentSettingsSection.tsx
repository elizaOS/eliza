/**
 * Provides the task-coordinator settings slot through the canonical Models &
 * Providers surface. Credentials remain owned by encrypted Accounts flows and
 * coding policy writes use the validated `/api/models/config` contract.
 */
export { ProviderSwitcher as CodingAgentSettingsSection } from "@elizaos/ui/components/settings/ProviderSwitcher";
