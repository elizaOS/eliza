/**
 * Type-only stand-in for dynamic `@elizaos/app` imports.
 *
 * Source path mappings can pull this package into local typecheck; dynamic
 * import sites cast the module shape explicitly and do not require generated
 * app dist output.
 */

export {};
