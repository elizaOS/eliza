# elizaos-plugin-gmail-watch (Python)

Gmail Pub/Sub watcher plugin for elizaOS. Spawns `gog gmail watch serve` and auto-renews the watch periodically.

## Installation

```bash
pip install elizaos-plugin-gmail-watch
```

## Development

```bash
pip install -e ".[dev]"
pytest
```
