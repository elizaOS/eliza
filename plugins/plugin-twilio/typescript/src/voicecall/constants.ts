/** Canonical service name for the voice call plugin */
export const VOICE_CALL_SERVICE_NAME = "voice-call";

/** Plugin version */
export const VOICE_CALL_VERSION = "2.0.0-alpha.1";

/** Plugin description */
export const VOICE_CALL_DESCRIPTION = "Voice call integration for elizaOS agents";

/** Default webhook port */
export const DEFAULT_WEBHOOK_PORT = 3334;

/** Default webhook path */
export const DEFAULT_WEBHOOK_PATH = "/voice/webhook";

/** Default media stream path */
export const DEFAULT_STREAM_PATH = "/voice/stream";

/** Maximum message length for TTS */
export const MAX_TTS_MESSAGE_LENGTH = 4096;

/** Default ring timeout (ms) */
export const DEFAULT_RING_TIMEOUT_MS = 30000;

/** Default max call duration (seconds) */
export const DEFAULT_MAX_DURATION_SECONDS = 300;

/** Default silence timeout for speech detection (ms) */
export const DEFAULT_SILENCE_TIMEOUT_MS = 800;

/** Default transcript timeout (ms) */
export const DEFAULT_TRANSCRIPT_TIMEOUT_MS = 180000;
