# Files that CANNOT appear in an LCOV report, so the enforced changed-file
# coverage gate must not hard-fail them as MISSING (#16409). The gate prints a
# visible "EXCLUDED (cannot appear in LCOV)" line for these instead — fail-open
# but auditable, and this manifest is code-reviewed like any source change.
#
# Every entry needs a reason and is meant to be DELETED: the real fix is making
# coverage collection work on the file. If an entry's file starts appearing in
# LCOV again, the gate automatically resumes gating it (presence always gates;
# only absence is excused).
#
# Rolldown coverage instrumentation fails on these inline type-import files.
# The vitest base config (packages/test/vitest/default.config.ts) carries
# matching coverage.exclude entries — note those still use a stale `eliza/`
# wrapper-repo path prefix, so the files' LCOV absence in practice comes from
# the collection failure itself, not the config. Either way: structurally
# absent until the Rolldown issue is fixed.
packages/agent/src/api/server.ts
packages/agent/src/runtime/eliza.ts

# Interface/type-only module: zero executable statements, so neither bun nor
# vitest emits an LCOV record for it (#16565 touched its SandboxCreateConfig
# doc surface). Delete this entry if the file ever gains runtime code.
packages/cloud/shared/src/lib/services/sandbox-provider-types.ts

# These adapters intentionally execute generated Smithers worker programs in a
# separate Bun process. The parent Vitest LCOV collector cannot instrument the
# worker source embedded in these files, even though their focused subprocess
# lifecycle suites execute the paths end to end. Remove once child-process LCOV
# is merged into the plugin coverage lane.
plugins/plugin-agent-orchestrator/src/services/smithers-task-runner.ts
plugins/plugin-workflow/src/services/smithers-runtime.ts

# Renderer composition entrypoint: the app Vitest coverage policy collects only
# `src/**/*.ts`, not TSX entrypoints, and importing this file boots the full UI.
# Product behavior is covered by packaged desktop acceptance; focused helpers
# still require changed Vitest coverage. Remove if TSX entrypoints enter LCOV.
packages/app/src/main.tsx

# Scaffolding asset copied into generated projects: importing it in the
# repository boots a React application rather than exercising the CLI template
# contract, so it cannot appear in the package's in-process LCOV. The real
# generated-project build and Biome contracts cover this asset end to end.
packages/elizaos/templates/min-project/src/index.tsx

# Public `@elizaos/agent` barrel: a pure re-export surface (`export *` /
# `export { ... } from ...`) whose own executable code is only the four lazy
# cloud-route wrappers. Its transitive graph re-exports the already-excluded
# `api/server.ts` and `runtime/eliza.ts` entrypoints AND `export *`s
# `./api/index.ts`, which pulls unbuilt sibling plugin dists
# (@elizaos/plugin-app-manager, etc.). The changed-files coverage lane runs
# BEFORE the workspace build, so those dist entrypoints are absent and the
# barrel cannot be imported in-process to instrument (same class as the two
# entrypoints it re-exports). The retention logic this stack added lives in
# runtime/memory-retention*.ts and runtime/eliza-plugin.ts, which ARE unit-
# tested for real. Remove this line if the barrel's export graph stops
# transitively requiring built plugin dists in the pre-build coverage lane.
packages/agent/src/index.ts

# CI check-script (UI-determinism classifier), not a shippable product module
# and never imported by any Vitest/Bun suite, so it structurally cannot appear
# in the changed-files LCOV report. It is exercised for real by its own
# `--self-test` (package.json `audit:ui-determinism:self-test`) plus the full
# `audit:ui-determinism` run in the Quality/Develop gate lane, both of which
# assert the classifier's behavior directly. Delete this entry if the script is
# ever refactored so its logic lives in a Vitest-instrumented module.
packages/scripts/audit-ui-determinism.mjs

# Dev-process orchestrators (Node-hosted Vite launch, #16892): importing either
# file immediately kills stale port owners, reserves ports, and spawns real
# long-lived dev servers (API + Vite) as top-level side effects, so no
# Vitest/Bun suite can ever import them and they structurally cannot appear in
# the changed-files LCOV report — same class as
# packages/scripts/audit-ui-determinism.mjs above. The launch-command logic
# they share is factored into packages/app-core/scripts/lib/dev-ui-vite.mjs,
# which IS instrumented (100% in this stack's changed-file LCOV) via
# packages/app/scripts/lib/dev-vite-command.test.mjs. Delete these entries if
# the orchestrators are ever refactored so their remaining logic lives in an
# importable, test-instrumented module.
packages/app-core/scripts/dev-ui.mjs
packages/app/scripts/dev-shared.mjs

# Real-voice lane entrypoints + adapter (#16945): every one of these executes
# through the fused native `libelizainference` (bun:ffi dlopen) with staged
# multi-GB GGUF bundles. The scripts are top-level CLI entrypoints — importing
# one in a unit suite immediately probes for the native lib and process.exit()s
# — and the workbench adapter's every code path acquires real fused
# encoder/diarizer/Kokoro handles, so none of them can execute inside the
# Vitest/Bun unit lanes where the native lib does not exist. They are exercised
# for real, end to end, by the self-provisioning voice-live-e2e nightly
# (roundtrip + acoustic-matrix jobs run each entrypoint against the real
# models in require-real mode; green run 29991510871). The pure logic they
# gained in #16945 is factored into
# plugins/plugin-local-inference/src/services/voice/kokoro/kokoro-ttfa-budget.ts,
# which IS instrumented via kokoro-ttfa-budget.test.ts. Delete an entry if its
# file is ever refactored so the remaining logic lives in an importable,
# test-instrumented module.
packages/app-core/scripts/voice-attribution-smoke.ts
packages/benchmarks/voice/voice-real-ci-matrix.mjs
plugins/plugin-local-inference/scripts/asr-real-smoke.ts
plugins/plugin-local-inference/scripts/kokoro-real-smoke.ts
plugins/plugin-local-inference/scripts/real-agent-voice-overlap.ts
plugins/plugin-local-inference/scripts/real-robustness-asr.ts
plugins/plugin-local-inference/scripts/real-voice-stack.ts
plugins/plugin-local-inference/scripts/voice-bench-shared.ts
plugins/plugin-local-inference/src/services/voice/workbench-real-services.ts
