/** Re-exports the shared embedding provenance contract for runtime consumers. */
export {
	BGE_SMALL_VECTOR_SPACE,
	copyEmbeddingVectorSpace,
	getEmbeddingVectorSpace,
	identifyEmbeddingVector,
} from "@elizaos/common";
