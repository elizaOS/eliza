# @elizaos/shared

Shared contracts, configuration, utilities, and assets used by runtime hosts, UI, Cloud services, and plugins.

Owns the first-party catalog in `src/catalog/`. Keep host assembly and rendering out of shared utilities; do not introduce dependencies on app or agent.

Build, test, and setup: [README.md](README.md).
