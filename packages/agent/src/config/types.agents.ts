/**
 * Config-scoped re-export of @elizaos/core; consumers import the agent config
 * types (AgentConfig and related shapes) through this local path.
 */
export * from "@elizaos/core/config/types.agents";
