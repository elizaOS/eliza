{
  "name": "@elizaos/confidant",
  "version": "2.0.0-alpha.475",
  "description": "Single seam for credential storage, mediation, and audit in Eliza agents.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/elizaos/eliza.git",
    "directory": "packages/confidant"
  },
  "engines": {
    "node": ">=22"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "bunx @biomejs/biome check src test",
    "lint:fix": "bunx @biomejs/biome check --write src test",
    "format": "bunx @biomejs/biome format src test",
    "format:fix": "bunx @biomejs/biome format --write src test"
  },
  "dependencies": {
    "@napi-rs/keyring": "^1.1.6"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "typescript": "^6.0.0",
    "vitest": "^4.0.0"
  }
}
