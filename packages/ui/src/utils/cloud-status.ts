/**
 * Re-exports the shared cloud-status predicates (authenticated / api-key-only).
 */
export {
  isCloudStatusAuthenticated,
  isCloudStatusReasonApiKeyOnly,
} from "@elizaos/ui/utils/cloud-status";
