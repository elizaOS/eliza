/**
 * Recording / STT logic for voice chat — transcript merging and normalization.
 */
export declare function normalizeTranscriptWord(word: string): string;
export declare function mergeTranscriptWindows(
  existing: string,
  incoming: string,
): string;
//# sourceMappingURL=voice-chat-recording.d.ts.map
