/**
 * Shared helpers and constants for StreamView.
 */
/** PIP window dimensions (640x360 → captures at 1280x720 on Retina 2x displays). */
export declare const PIP_SIZE: {
    width: number;
    height: number;
};
export declare const FULL_SIZE: {
    width: number;
    height: number;
};
/** Detect popout mode from URL. */
export declare const IS_POPOUT: boolean;
//# sourceMappingURL=helpers.d.ts.map