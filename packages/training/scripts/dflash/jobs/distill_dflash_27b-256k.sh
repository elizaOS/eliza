#!/usr/bin/env bash
# Hidden placeholder only. The active Eliza-1 release line is 0_8b / 2b / 4b.
# Do not run larger-tier DFlash distillation until those tiers are formally
# reintroduced with final weights, evals, licenses, and platform evidence.
echo "$(basename "$0") is disabled: active Eliza-1 tiers are 0_8b, 2b, 4b." >&2
exit 2
