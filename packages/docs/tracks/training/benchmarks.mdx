---
title: Benchmarks
description: "70+ harnesses for agents, models, and embodied systems."
---

The benchmarks suite orchestrates evaluation runs across every Eliza subsystem. It covers tool-call correctness, agent autonomy, voice and vision, embodied control, long-horizon reasoning, and adversarial robustness.

## Categories

- **Eliza-1** — model-side: tool-calling, instruction following, agentic loops.
- **SWE-Bench** — software engineering tasks.
- **LifeOps-Bench** — long-running personal-assistant scenarios.
- **Orchestrator** — multi-agent coordination harness.
- **Vision-Language** — multimodal grounding.
- **Voice-Emotion** — speech recognition and affect under noise.
- **Feed / ScamBench** — adversarial social pressure inside the Feed simulation.
- **Robot** — embodied skill evaluation.

## Running a benchmark

The benchmarks now live in the standalone [elizaOS/benchmarks](https://github.com/elizaOS/benchmarks) repository. The orchestrator (`orchestrator/` in that repo) accepts a config and runs the named harness, collecting traces and metrics. Configs pin the model under test, dataset shard, evaluation criteria, and reporter.

## Reports

Each run emits a calibration report, scorecards, and per-task traces. Reports are checked into the benchmarks repo (`reports/`) for reference runs.

## Source

- Suite root: [github.com/elizaOS/benchmarks](https://github.com/elizaOS/benchmarks)

## See also

- [Eliza-1](/tracks/training/eliza-1)
- [Feed](/tracks/training/feed)
- [Robot](/tracks/training/robot)
