{
  "name": "memperf-suite",
  "private": true,
  "type": "module",
  "description": "Desktop/server memory-benchmark harness for eliza-1 tiers (measures an ELIZA_REPO_DIR checkout).",
  "scripts": {
    "bench": "node run-all.mjs",
    "bench:json": "node run-all.mjs --json",
    "test": "bun test metric-schema.test.ts",
    "typecheck": "tsgo --noEmit -p tsconfig.check.json"
  },
  "devDependencies": {
    "@elizaos/plugin-local-inference": "workspace:*",
    "@elizaos/shared": "workspace:*",
    "@types/bun": "^1.2.0",
    "@types/node": "^24.0.0"
  }
}
