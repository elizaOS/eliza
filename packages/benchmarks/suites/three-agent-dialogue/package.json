{
  "name": "@elizaos/three-agent-dialogue",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "End-to-end three-agent dialogue harness \u2014 three Eliza agents, distinct voices, shared audio bus, diarization + emotion + turn-detection capture.",
  "main": "./runner/run-dialogue.ts",
  "types": "./runner/run-dialogue.ts",
  "scripts": {
    "bench": "bun run runner/run-dialogue.ts",
    "bench:smoke": "THREE_AGENT_SMOKE=1 bun run runner/run-dialogue.ts",
    "test": "bun x vitest run --config vitest.config.ts",
    "test:watch": "bun x vitest --config vitest.config.ts",
    "lint": "bunx @biomejs/biome check --write --unsafe .",
    "lint:check": "bunx @biomejs/biome check .",
    "format": "bunx @biomejs/biome format --write .",
    "format:check": "bunx @biomejs/biome format .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@elizaos/core": "workspace:*",
    "@elizaos/plugin-groq": "beta"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "bun-types": "1.3.14",
    "@typescript/native": "npm:typescript@^7.0.2",
    "typescript": "^6.0.3",
    "vitest": "4.1.10"
  }
}
