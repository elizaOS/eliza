/**
 * Public entry for `@elizaos-benchmarks/lib`.
 *
 * Re-exports the canonical metrics schema. Future shared helpers (delta
 * computation, aggregator utilities, etc.) land here in later waves.
 */

export * from "./metrics-schema.ts";
