[project]
name = "elizaos-benchmarks-realm"
version = "0.2.0"
description = "REALM-Bench: Real-World Planning Benchmark for LLMs and Multi-Agent Systems (elizaOS implementation)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "elizaOS Team" }
]
keywords = ["realm-bench", "benchmark", "planning", "llm", "multi-agent", "elizaos"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

# OR-Tools is REQUIRED. The optimality scoring for JSSP / TSP-TW / DARP all
# depend on CP-SAT or RoutingModel; without OR-Tools the oracle would only
# produce a loose lower bound (JSSP) or a greedy upper bound (DARP), which
# turns "optimality_ratio" into a meaningless number. We pin a recent
# version because the RoutingModel API has stabilised since 9.x.
dependencies = [
    "ortools>=9.5,<10.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
]

[project.scripts]
realm-bench = "benchmarks.realm.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["benchmarks"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
