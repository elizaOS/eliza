{
  "name": "@elizaos/bench-eliza-1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Quality + perf bench for eliza-1 (unguided vs guided GBNF decode) compared against Claude Haiku across the response handler, planner, and per-action tasks.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./metrics": "./src/metrics.ts",
    "./report": "./src/report.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "bunx @biomejs/biome check .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.95.2"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^6.0.3",
    "vitest": "4.1.5"
  }
}
