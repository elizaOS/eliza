/** Preserves the app-script import path for the canonical native plugin discovery helper. */
export {
  CAPACITOR_PLUGIN_NAMES,
  NATIVE_PLUGINS_ROOT,
  resolveNativePluginDir,
  resolveNativePluginDir as nativePluginDir,
} from "./lib/capacitor-plugin-names.ts";
