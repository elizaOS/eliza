# Service Configuration
SERVICE_NAME="My Service"
SERVICE_DESCRIPTION="An MCP + A2A service"
SERVICE_VERSION="1.0.0"
PORT=3000

# Network Configuration
# Options: localnet, testnet, mainnet
JEJU_NETWORK=testnet

# RPC URLs (optional - defaults provided)
# LOCALNET_RPC_URL=http://localhost:8545
# TESTNET_RPC_URL=https://sepolia.base.org
# MAINNET_RPC_URL=https://mainnet.base.org

# ============================================================================
# x402 Payment Configuration
# ============================================================================
# By default, x402 uses ElizaOS Cloud for payment processing.
# No configuration needed for basic cloud-hosted payments.

# Set to your wallet address to receive payments directly
# If not set, payments are routed through ElizaOS Cloud
# PAYMENT_RECIPIENT=0xYourWalletAddress

# x402 Network (optional - auto-detected from JEJU_NETWORK)
# Options: base, base-sepolia, jeju, jeju-testnet
# X402_NETWORK=base-sepolia

# x402 Facilitator (optional - defaults to ElizaOS Cloud)
# Set to use your own facilitator instead of ElizaOS Cloud
# X402_FACILITATOR=https://pay-testnet.elizaos.ai/v1

# ============================================================================
# Authentication & Keys
# ============================================================================

# Private Key (required for ERC-8004 registration)
# NEVER commit this to git!
PRIVATE_KEY=0xYourPrivateKey

# ============================================================================
# Feature Flags
# ============================================================================

# x402 Payments (defaults to true, uses ElizaOS Cloud)
X402_ENABLED=true

# ERC-8004 Agent Registry Integration
ERC8004_ENABLED=true

# Auto-register to ERC-8004 on startup
# AUTO_REGISTER=true

# ============================================================================
# Visibility & Discovery (after registration)
# ============================================================================

# Agent ID (set this after running `bun run register`)
# Format: chainId:tokenId (e.g., 84532:123)
# AGENT_ID=84532:123

# Service visibility (default: true = public)
# Set to false to hide from marketplace listings
SERVICE_PUBLIC=true

# Service category for marketplace discovery
# Options: ai, compute, storage, game, api, defi, other
SERVICE_CATEGORY=service

# Public URL for endpoints (used by manage script)
# SERVICE_URL=https://your-service.example.com

# Contract Addresses (optional - defaults provided for testnet)
# IDENTITY_REGISTRY_ADDRESS=0x...
