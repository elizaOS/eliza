### Experimental: RLM Provider (WIP)

A scaffold for integrating Recursive Language Models (RLM) as an optional
reasoning backend. This is an early placeholder to define structure and
integration boundaries before implementation.
