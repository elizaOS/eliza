{
  "name": "@elizaos/computeruse-root",
  "private": true,
  "version": "2.0.0-alpha.77",
  "description": "ComputerUse - Desktop automation with AI",
  "scripts": {
    "test": "bun run test:bindings && bun run test:workflow",
    "test:bindings": "cd packages/computeruse-ts && bun run test",
    "test:desktop": "cd packages/computeruse-ts && bun run test:desktop",
    "test:workflow": "cd packages/workflow && npx jest --passWithNoTests || echo 'Workflow tests skipped (requires native module build)'"
  }
}
