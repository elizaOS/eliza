# Legacy Gateway Discord AWS Terraform

This is a quarantined copy of the old package-level Gateway Discord AWS Terraform root.

Use this directory for audit/reference only. The canonical, CI-managed AWS Terraform root is:

```text
cloud/services/gateway-discord/terraform
```

No repository workflow or local setup reference to this legacy root was found during the consolidation pass.
